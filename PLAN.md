# 続きの計画（2026-09-16 時点）

ステップ19（プロンプトインジェクションの実測）まで終わっている。README の「ステップ」番号を継ぐ。
**各ステップは実測を README に残すまでが1ステップ**、コミットは実装 → 提示 → レビューのあと。

## 次にやる順

### ステップ20: eval ハーネス

「実測を README に残す」を手作業でやっている。同じ質問を流して前後を比べる仕組みにする。

- `.hma/evals/*.json` に「入力 / 期待するツールの使い方 / 上限トークン」を書く
- `hma eval` で全部流し、ツール呼び出し・トークン・承認回数を表で出す
- 既存の telemetry（ClickHouse の `gate` / `usage`）をそのまま集計に使う
- **測るもの**: プロンプトを変えたときの回帰（例: ステップ15 で「任せた仕事を自分で
  やり直すな」を足す前後）

## そのあとの候補（順不同）

- **`.hma/agents/*.md`** — サブエージェントの定義を外出し。スキルと同じ形（frontmatter に
  name / description / 使える道具）。いまは `SUBAGENTS` がコードの中にある
- **Web ツール**（fetch / search）— 外の世界。ステップ19 の実地になる
- **画像を読む** — スクリーンショットを渡す。`content` の配列形式が MCP と同じ形で出てくる
- **履歴の分岐 / `--resume`** — 追記ログがあるので、途中から枝を生やせるはず
- **手動 compact（`/compact`）** — スラッシュコマンドの仕組みに乗る
- **料金の計算** — トークンではなく金額で出す。モデルごとの単価表が要る

## 積み残している穴（各ステップの「まだやっていないこと」から、重いもの）

| 穴 | どのステップ | いまの状態 |
|---|---|---|
| 実行中に落ちたツールは at-least-once | 13 | 「走ったかもしれない」と分かるところまで |
| background の子は落ちたら消える | 15 | ツール結果の文面で予防線を張っただけ |
| 子のイベントが実時間で出ない | 14 / 15 | ターンの境目で `drain` するだけ |
| `workspace` の閉じ込めが MCP に効かない | 17 | 権限ルールだけが効く |
| MCP は tools のみ・stdio のみ・再起動なし | 17 | — |
| 並列ツール実行のイベントが完了順に出ない | 18 | 結果が出揃ってから元の順に出す |
| 並列に走るツールの `drain` が共有の1本 | 18 | どのツールのイベントか区別できない |
| 設定の再読み込みをしない | 8 | 起動時に1回。入れないと決めた |
| `todo_write` の表示口が無い | 12 | AG-UI の `STATE_SNAPSHOT` が要る。入れないと決めた |

## 作法（忘れないように）

- 先に正解を実装しない。**素朴版で壊してから**対策を入れる
- 実測は同じ入力で前後を取る。**失敗した試行も残す**（一番の教材になる）
- コミットは1関心事。メッセージは日本語1行 + なぜ / 実測 / あえてやらなかったこと
- 無料枠は 5 RPM。並列で走らせると自分で 429 を踏む
