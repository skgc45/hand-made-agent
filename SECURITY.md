# セキュリティについて

**いまは**練習用の教材で、「素朴に実装すると何が壊れるか」を見せるために
**まだ塞いでいない穴がある**。踏んで理解するのが先で、塞ぐのが後、という順で進めている。

目指しているのは**個人開発で実用できるエージェント**なので、下の表は恒久的なものではない。
塞いだものから表を減らしていく。

## まだ塞いでいないもの（既知。報告不要）

[docs/notes.md](docs/notes.md) の該当する節で、それぞれ何が危ないかを説明している。

| | どこに書いてあるか |
|---|---|
| `bash` ツールは任意のコマンドを実行する。承認ゲートは UX であって防御ではない。`read_file(path)` の deny も `bash` 経由では守れない | ステップ2・7・19 |
| `workspace` はセキュリティ境界ではない。`bash` からも MCP からも外に出られる | ステップ2・17 |
| `.hma` のフックと MCP サーバは、信頼したあとは承認を通らずに実行される | ステップ10 |
| ツール結果・`AGENTS.md`・スキル本文・MCP のツール説明文はプロンプトインジェクションの入口 | ステップ11・16・17 |
| run の途中で落ちたツールは at-least-once（二重実行しうる） | ステップ13 |

## 塞いであるもの

| | |
|---|---|
| `hma serve` はループバックにだけ待ち受ける | 認証が無く、承認をクライアント自身が返すため |
| `POST /` は Content-Type / Origin / Host を見る | 他サイトからの CSRF と DNS リバインディング |
| リクエストボディは 1MB まで | |
| 承認 UI はツール名を `textContent` で出す | MCP のツール名は外部由来 |
| `threadId` は `[A-Za-z0-9_-]{1,128}` のみ | 保存先のパス移動 |
| API キーは設定ファイルから読まない | 共有される場所に鍵を書かせない |
| `.hma` の緩める方向の設定は初回に確認する | clone しただけで権限が緩まないように |

## 自動で見ているもの

- **gitleaks** — CI で履歴全体を毎回走査する。手元でも `pre-commit` で staged 差分を走査する
  （`pnpm install` が有効にする）。**gitleaks が未インストールならコミットを中止する** —
  黙って飛ばすと、走っているつもりで素通りする事故が起きるため
- **CodeQL** / **pnpm audit** — CI（push / PR / 毎週）

## 報告してほしいもの

「まだ塞いでいないもの」の表に無いもの。特に「ローカル前提のつもりが外に開いていた」類。

GitHub の Issue で報告してください。すぐ悪用できる内容なら、Issue ではなく
[Security advisory](https://github.com/skgc45/hand-made-agent/security/advisories/new) を使ってください。
